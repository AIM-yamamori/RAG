# フロントエンド関数（Streamlit）

## 記憶の箱
- 役割: 画面がリロードされても消したくないデータを保存する「記憶の箱」です。
- なぜ重要か: Streamlitはユーザーが文字を入力するたびに、プログラムを上から下まで全部実行し直す（リセットする）という性質があります。普通に変数のリスト（messages = []）を作ると、毎回中身が空っぽに戻ってしまいます。これを使うことで、過去の会話履歴をずっと残せるようになります。
```python
st.session_state
```
例:
```python
# 最初の一度だけ空のリスト（箱）を作る
if "chat_history" not in st.session_state:
    st.session_state.chat_history = []
```

## チャット入力欄
- 役割: 画面の一番下に表示される、おなじみの「チャット入力欄」を一瞬で作る関数です。
- なぜ重要か: HTMLやJavaScriptでの面倒なフォーム作成や「Enterキーが押されたら送信する」という処理を、この関数ひとつで自動でやってくれます。
```python
# ユーザーが文字を入力してEnterを押したら、変数「user_text」にその文字が入る
if user_text := st.chat_input("質問をどうぞ"):
    st.write(f"あなたが入力したのは: {user_text}")
```

## 会話の吹き出し
- 役割: チャット特有の「アイコン付きの吹き出し」を作る関数です。
- なぜ重要か: 吹き出しのデザインを自分で用意する必要がありません。"user"（人間）と "assistant"（AI）の2つのキーワードを指定するだけで、自動で見やすい左右のレイアウトやアイコンに切り替わります。
```python
with st.chat_message("user"):
    st.write("こんにちは")  # 人間の吹き出しになる

with st.chat_message("assistant"):
    st.write("私はAIです")  # AIの吹き出しになる
```

## ローディング表示
- 役割: AIが回答を考えている間（データベースを検索している間）、画面に「ぐるぐる（読み込み中）」のアイコンを表示する関数です。
- なぜ重要か: AIの回答生成には数秒〜10秒ほど時間がかかります。これがないと、ユーザーは「アプリがフリーズしたのかな？」と不安になってしまいます。親切なアプリ作りに欠かせない関数です。
```python
with st.spinner("AIが考え中..."):
    result = answer_question(question) # 時間がかかる処理
# 処理が終わると自動でぐるぐるが消えます
```