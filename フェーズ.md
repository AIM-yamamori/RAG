# RAGプロジェクト フェーズ

## フェーズ一覧
| フェーズ | 内容 |
| --- | --- |
| フェーズ1 | 設計(構成・ディレクトリ・データフロー決定) |
| フェーズ2 | GCP環境構築(プロジェクト・予算アラート・API有効化・GCS) |
| フェーズ3 | ローカル開発環境(Python 3.12・gcloud・仮想環境・.env) |
| フェーズ4 | Cloud SQL構築(インスタンス・pgvector・テーブル作成) |
| フェーズ5 | データ取り込みパイプライン(chunker・embedder・ingest) |
| フェーズ6 | バックエンド実装(db・llm_client・rag_service) |
| フェーズ7 | Streamlit UI実装・ローカル動作確認 |
| フェーズ8 | Cloud Runデプロイ・公開URL確認 |

---

## フェーズ1: 設計

### 完成形

以下4つの設計書が揃い、「何を・どう作るか」を誰かに説明できる状態。

- 要件定義書
- 概要設計書
- 基本設計書
- 詳細設計書

### 完成させるために必要なこと

| 必要なこと | 内容 |
|---|---|
| データの棚卸し | 就業規則11ファイル、82条文を確認する |
| 技術選定 | ベクトルDB・Embedding・LLM・UIを決める |
| コスト方針の決定 | 無料クレジット内に収める方針を固める |

### 使用技術・概念

| 技術/概念 | 役割 |
|---|---|
| RAG(Retrieval-Augmented Generation) | 検索と生成を組み合わせる手法そのもの |
| アーキテクチャ設計 | 何をどの順番でつなぐかを決める思考法 |

---

## フェーズ2: GCP環境構築

### 完成形

以下が全て揃っている状態。

- プロジェクトが作成されている
- 予算アラートが設定されている
- 必要なAPIが全て有効化されている
- Gemini APIキーが取得できている
- 就業規則ファイルがGCSにアップロードされている

### 完成させるために必要なこと

| 必要なこと | 確認コマンド |
|---|---|
| プロジェクト作成 | `gcloud config list` でproject確認 |
| API有効化 | `gcloud services list --enabled` |
| APIキー取得 | Google AI Studioで発行(`AIzaSy`または`AQ.`で始まる) |
| GCSアップロード | `gcloud storage ls gs://バケット名/company_rule/` で11ファイル確認 |

### 使用技術・概念

| 技術/概念 | 役割 |
|---|---|
| GCPプロジェクト | 課金・権限・リソースの管理単位 |
| IAM(Identity and Access Management) | 誰が何をできるかを制御する仕組み |
| Cloud Billing予算アラート | 想定外課金を検知する仕組み |
| Google Cloud Storage(GCS) | ファイルを置いておくクラウドストレージ |
| Gemini APIキー | Gemini APIを呼び出すための認証情報 |

---

## フェーズ3: ローカル開発環境構築

### 完成形

ローカルPCで Python コードが実行できる状態。

- Python 3.12がインストールされている
- 仮想環境が作成されている
- 必要なライブラリが全てインストールされている
- .envにAPIキー等が設定されている

### 完成させるために必要なこと

| 必要なこと | 確認コマンド |
|---|---|
| Python 3.12 | `python --version` |
| 仮想環境有効化 | プロンプトに`(venv)`表示 |
| ライブラリ | `pip install -r requirements.txt` がエラーなく完了 |
| .env設定 | `GEMINI_API_KEY`、`GOOGLE_CLOUD_PROJECT`が入っている |

### 使用技術・概念

| 技術/概念 | 役割 |
|---|---|
| Python仮想環境(venv) | プロジェクトごとにライブラリを隔離する仕組み |
| pip / requirements.txt | ライブラリのバージョン管理 |
| .env / python-dotenv | 秘匿情報をコードから分離する仕組み |
| gcloud CLI | ローカルからGCPを操作するコマンドラインツール |

---

## フェーズ4: Cloud SQL構築

### 完成形

ベクトルを格納する空のテーブルが用意できている状態。

- Cloud SQLインスタンスが`RUNNABLE`
- pgvector拡張が有効
- `rule_chunks`テーブルが作成済み(まだ0件)

### 完成させるために必要なこと

| 必要なこと | 確認コマンド |
|---|---|
| インスタンス作成 | `gcloud sql instances describe rag-rules-db --format="value(state)"` → RUNNABLE |
| pgvector有効化 | Cloud SQL Studioで`CREATE EXTENSION vector;` |
| テーブル作成 | `SELECT COUNT(*) FROM rule_chunks;` → 0(エラーが出ない) |

### 使用技術・概念

| 技術/概念 | 役割 |
|---|---|
| Cloud SQL for PostgreSQL | マネージド型リレーショナルデータベース |
| pgvector拡張 | PostgreSQLにベクトル型・類似検索機能を追加する拡張 |
| VECTOR型 | 浮動小数点数の配列を専用に扱うデータ型 |
| ivfflatインデックス | ベクトル検索を高速化する索引方式 |

---

## フェーズ5: データ取り込みパイプライン実装

### 完成形

就業規則82条文が、意味のあるベクトルとしてCloud SQLに格納されている状態。

- `rule_chunks`テーブルに82件のレコードがある
- 異なる条文が異なるベクトルを持っている(全部同じベクトルではない)

### 完成させるために必要なこと

| 必要なこと | 内容 |
|---|---|
| chunker.py | Markdownを条文単位に分割する |
| embedder.py | テキストをベクトル化する(**Gemini API使用**) |
| ingest.py | 上記を統合してCloud SQLに書き込む |
| 実行 | `python -m ingest.ingest` を1回実行する |
| 検証 | `SELECT COUNT(*) FROM rule_chunks;` → 82 |

### 使用技術・概念

| 技術/概念 | 役割 |
|---|---|
| 正規表現(re) | テキストからパターンを検出・分割する |
| チャンク分割(Chunking) | 長い文書を検索に適した単位に切り分ける |
| Embedding(埋め込み) | テキストをベクトルに変換する処理 |
| Gemini Embedding API | Googleが提供するEmbedding生成サービス |
| SQLAlchemy | Pythonからデータベースを操作するライブラリ |
| Cloud SQL Python Connector | Cloud SQLへの安全な接続を確立するライブラリ |

---

## フェーズ6: バックエンド実装

### 目的別完成形

**検索(Retriever)の完成形**

質問文を投げると、関連する条文が高い類似度(0.8以上)で返ってくる状態。

```
「年次有給休暇は何日もらえますか？」
→ 第28条(年次有給休暇) 類似度: 0.86
```

**回答生成(Generator)の完成形**

検索結果をもとに、Geminiが自然な日本語で回答を生成する状態。

```
「勤続6か月で10日、6年6か月以上で最大20日の
年次有給休暇が付与されます。」
```

### 完成させるために必要なこと

| モジュール | 必要なこと |
|---|---|
| db.py | pgvectorのコサイン類似度検索(`<=>`演算子)を実装する |
| llm_client.py | Geminiにプロンプトを送り回答を得る(**gemini-2.5-flash-lite使用**) |
| rag_service.py | 検索→フィルタ→生成の一連の流れを統合する |
| テスト | `test_search.py`、`test_rag.py`で単体・統合確認する |

### 使用技術・概念

| 技術/概念 | 役割 |
|---|---|
| コサイン類似度 | 2つのベクトルの「向き」の近さを測る指標 |
| プロンプトエンジニアリング | LLMへの指示文を設計する技術 |
| pg8000 | PostgreSQL用の軽量Pythonドライバ |
| リトライ処理 | 一時的なエラーに対して再試行する設計 |
| Gemini Generative API | テキスト生成を行うLLM API |

---

## フェーズ7: Streamlit UI実装・ローカル動作確認

### 完成形

ブラウザ(`localhost:8501`)からチャット形式で質問でき、回答と出典が表示される状態。

```
👤 年次有給休暇は何日もらえますか？

🤖 勤続6か月で10日、6年6か月以上で最大20日の
   年次有給休暇が付与されます。
   ▼ 出典: 04_休暇制度.md 第28条(年次有給休暇)
```

### 完成させるために必要なこと

| 必要なこと | 内容 |
|---|---|
| main.py | `st.chat_input`、`st.chat_message`、`st.session_state`でUI構築 |
| 起動確認 | `streamlit run app/main.py` でエラーなく起動 |
| 複数質問確認 | 年休・試用期間・出張手当など複数パターンで確認 |
| フォールバック確認 | 無関係な質問で「該当情報なし」と返る |

### 使用技術・概念

| 技術/概念 | 役割 |
|---|---|
| Streamlit | PythonのみでWebアプリを作るフレームワーク |
| st.session_state | ブラウザのリロードをまたいでも保持される状態管理 |
| st.chat_message / st.chat_input | チャットUI専用のコンポーネント |

---

## フェーズ8: Cloud Runデプロイ

### 完成形

公開URL(`https://rag-rules-chatbot-xxxx.run.app`)にブラウザでアクセスすると、ローカルと同じチャット画面が表示され、正常に動作する状態。

### 完成させるために必要なこと

| 必要なこと | 内容 |
|---|---|
| Dockerfile | Streamlit起動用のコンテナ定義 |
| ローカルDocker動作確認 | `docker run`でコンテナが動く |
| イメージビルド | Cloud Buildでコンテナイメージを作成 |
| Artifact Registry権限 | Cloud Runがイメージをプルできる権限 |
| Cloud SQL接続権限 | `roles/cloudsql.client` |
| **Cloud Runへのデプロイ成功** | `gcloud run deploy` が正常終了する |
| 環境変数設定 | GEMINI_API_KEY、GOOGLE_CLOUD_PROJECT |
| 公開URL動作確認 | ブラウザでチャットが機能する |

### 使用技術・概念

| 技術/概念 | 役割 |
|---|---|
| Docker / Dockerfile | アプリを実行環境ごとパッケージ化する技術 |
| Cloud Build | ソースコードからコンテナイメージをビルドするサービス |
| Artifact Registry | ビルドしたコンテナイメージを保管する場所 |
| Cloud Run | コンテナを実行するサーバーレス基盤 |
| IAM(サービスアカウント権限) | Cloud Runが他サービスにアクセスする許可 |
| 環境変数 | コンテナ起動時に渡す設定値 |